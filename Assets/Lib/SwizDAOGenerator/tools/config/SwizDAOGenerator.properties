####################################### VERSION 1.0 ###################################
Generator.author           = NS Devaraj

################################ TEMPLATE PROPERTIES ##################################
templates.dir			= templates/
templates.assets.dir	= templates/assets
SwizDAO.templates.dir = templates/SwizDAO

################################ DIRECTORIES ################################
model.dir                 = model
view.dir                  = view
processor.dir             = processor
vo.dir                    = vo
mediators.dir              = mediators 
renderers.dir              = renderers 
control.dir              = control 
assets.dir				 = assets
################################ PROTECTED PROPERTIES #################################
namespace           = ${com.dir}.${domain.dir}.${project.dir}
project.main.url    = ${workspace.dir}/${project.dir}/${src.dir}
project.root.url    = ${workspace.dir}/${project.dir}/
project.url         = ${workspace.dir}/${project.dir}/${src.dir}/${project.path}